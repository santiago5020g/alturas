{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nivel_certificado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nivel_certificado.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ nivel_certificado.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('nivel_certificado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('nivel_certificado_edit', { 'id': nivel_certificado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
